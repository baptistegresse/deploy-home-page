{
  "name": "auto-homepage",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun server.js",
    "dev": "bun --hot server.js",
    "build": "echo 'No build step required'",
    "test": "bun run test-simple.js",
    "test-site": "bun run test-site-complet.js",
    "test-webflow": "bun run test-webflow-deploy.js"
  },
  "dependencies": {},
  "engines": {
    "bun": ">=1.0.0"
  }
}
